import { View, TouchableOpacity } from 'react-native';
import Icon from '../../public/Icon';

const Header = () => {
  return (
    <View>
      <TouchableOpacity>
        <Icon />
      </TouchableOpacity>
    </View>
  );
};

export default Header;
